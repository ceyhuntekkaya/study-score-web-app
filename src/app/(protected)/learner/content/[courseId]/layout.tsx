'use client';

/**
 * Course Layout
 * This layout is specific to [courseId] routes
 * It inherits from parent content layout
 */
export default function CourseLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}

