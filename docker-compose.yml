version: "3.8"

services:
  app:
    container_name: speaking-exam-admin
    image: ctekkaya/study-score-app:2.0
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "3000:3000"
